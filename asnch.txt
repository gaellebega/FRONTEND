the 
 "https://jsonplaceholder.typicode.com/todos/?t=" + new Date().getTime()
); that new date is used to prevent the caseh which wa giving use the error 304 to make it to success of 200 sot hat everything can now fall into place  to 200 OK which is a success

in the response is where we have all our data

//HOW DO WE ACCESS OUR DAT THEN?

//stages of the request

| State | Meaning                          |
|                                          |
| 0  UNSENT    | You haven’t ordered yet   |
| 1   OPENEND request.open | You placed the order      |
| 2   HEADERS_RECEIVED  request.send| Restaurant accepted order |
| 3   LOADING  | Food is being cooked      |
| 4   DONE     | Food is delivered         |
good combo usage when the request is 4 then get error 200 like everything is now done

Informational responses (100 – 199)
Successful responses (200 – 299)
Redirection messages (300 – 399)
Client error responses (400 – 499)//errors in the browser like wrong end point and so and so on
Server error responses (500 – 599)


JSON FORMAT
this is the way that data are written as the js objects in [] but theya re absically it is the strings
=>it has to be the strings cz when the browser have to change the data it has to be in the strings format

javascriot object notation JSON
the difference between the normal object and the json object is this the json objects can not be writtn in the single quotes they are always the double quotes
but the numbers do not have to be in the double quotes and boolean values


CHAINING THE PROMISE 

.finally() does NOT print the latest message.
It prints its own message because it does not receive the previous value.

for the .catch we only need to have one so that we can run it everytime we see the error
