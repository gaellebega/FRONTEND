JUSTFY CONTENT move alll together divs along the maain axis_
it is always horizantal
it works with the width
mainly used for the logos and the menus

justify-start
justify-center
justify-end
justify-between
justify-around
justify-evenly



ALIGN ITEMS
ALSO USED IN FLEX ROWS | it works with height

items-start
items-end
items-center
items-end
items stretch

width:100%
height:100vh
w-full
h-screen
w-1/2
1-1/3
padding is inside the box (PIN)
margin is outside of the box (MOMO)

align content

it works when you have the multiple rows with the flex wrap

flex-row it is the default flex
items-height
flex flex-col to make it vertical

ALIGN CONTENT
Used when we have many divs of rows inside the one div


when you choose to use the content-center alone then make sure also to use the flex-wrap
so think of it like the computer can not use the content alone when there is no multiple content inside it
content-center
content-evenly
content-around
content-between
content-stretch
content-start
content-end


when you used justify-center on the height then it take that element as one
when also you use the content-align on width it take it as one



GRIDS IN USAGE
grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4


// 1. Select
const parent = document.getElementById("container");

// 2. Create
const element = document.createElement("h1");

// 3. Modify
element.textContent = "Hello";

// 4. Add / Remove / Replace
parent.appendChild(element);


the way to use the query selector when you dont want to select all like selct one of the items you makek it like this
document.querySelcetorAll("element.class-name")


TASK
CREATE THE SMALL USER PROFILE WHERE THE USER CAN EDIT NAME 

CRUD
DELETE PRODUCT to the cart
REMOVE PRODUCT to the cart
MODIFY THE PRODUCTS to the cart
ADD THE PRODUCTS to the cart

dynamic html content
this referes to the html content that change when the js is init


changing the css this is how everythign is made
document.getElementById(id).style.property= new style

html allow only one oneclick on the attribute

for the tailwind to work you have to put in this <script src="https://cdn.tailwindcss.com"></script>


the common validation attributes are 
<input required>          <!-- Must not be empty -->
<input minlength="3">    <!-- At least 3 characters -->
<input maxlength="20">   <!-- Maximum 20 characters -->
<input type="email">     <!-- Must look like an email -->
<input type="number">    <!-- Must be a number -->
<input min="1">          <!-- Minimum value -->
<input max="100">        <!-- Maximum value -->


GET used to get the data from the server
POST used to create the data from the server
PUT used to replace the exsting data from the server
DELETE used to delete the data from the server

put and delete and patch mainly uses the js
for post it is seen in the url that is why when you search you can be able to see
/?search=phone

REMEBER TO ADD THE defer in ALL THE SCRIPT OF YOUR JS FILE SO THAT  when you have more than one files the browser have to lad them all at the same time


keydown this is all the events that might happen weh you touch they key
keyup this refers to all the evenets that might happen when you finish to type


if you are getting the git warning then you have to run this
*text=auto eol=lf
if that is not working then we have to write something like this
git add --renormalize .


padstart is js method that is used to add the characters to the beginning og a string until it reaches a specified lenth

string.padStart(targetlenth,padstring)
let num="3";
//this means go then take the existing num then in front of it add number 0 so that the length becomes 2
console.log(num.padStart(2,"0"));

TIMER FUNCTIONS

setTimeout() set a clock timeout(runs once): wait until amount of time then the code runs
setInterval() sets a clock interval(runs repeatedy):runs code after every time speciied ex ater every sec a clock ticks
clearTimeout() clears the tieout
clearInterval() this clears an interval

Event Management is about adding, removing, and controlling events.


the way the removeEventListener is used
element.removeEventListener(eventType, functionName);
the role is to remove the event which was previously there

youc an give the button type like reset submit and button

required on the inputs is used to give use the default form validation
document.Forms(); returning all the form elements of the document
.value to change the input field
document.tittle(); used to get the title of the form

WE USE the toggle for the on and off
we use the 
sample page of all that features you have said of toggle feautures?